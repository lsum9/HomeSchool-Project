<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.home_school.login.mapper.LoginMapper">
  <select id="signCheck" parameterType="LoginUserDto" resultType="int">
      SELECT
          COUNT(user_code)
      FROM tb_user
      WHERE user_code=#{sub}
  </select>

    <insert id="signUp" parameterType="LoginUserDto">
        INSERT INTO
            homeschool.tb_user
            (
             user_id
            , user_type
            , user_status
            , frst_wrt_id
            , last_wrt_id
            , user_code
            )
        VALUES
            (
             #{email}
            ,'USER'
            ,'y'
            ,#{email}
            ,#{email}
            ,#{sub}
             );
    </insert>

    <update id="updateUser" parameterType="LoginUserDto">

    </update>

</mapper>
